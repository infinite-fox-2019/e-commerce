'use strict'

const router = require('express').Router()
const { TransactionController } = require('../controllers')
// const { authorization } = require('../middlewares/authorization')

// router.get('/', TransactionController.findMine)
// router.get('/all', authorizeAdmin, TransactionController.findAll)
// router.patch('/:id', authorize, TransactionController.shipped)

module.exports = router
