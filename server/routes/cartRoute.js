const router = require('express').Router()
// const CartController = require('../controllers/cartController')
// const { authentication } = require('../middleware/auth')

// router.post('/',authentication, CartController.create)
// router.patch('/:id', authentication, CartController.updateStatus)
// router.get('/status', authentication, CartController.findByStatus)
// router.get('/', authentication, CartController.find)
// router.get('/:id', authentication,  CartController.findById)

module.exports = router